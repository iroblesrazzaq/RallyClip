{
  "_meta": {
    "note": "Scope ledger. `passing` requires running the verification command in-session with evidence. Update in the same commit as the work.",
    "py": "/Users/ismaelrobles-razzaq/2_cs_projects/rallyclip_container/RallyClip/.venv-train/bin/python3",
    "run_from": "repo root, PYTHONPATH=src:tests"
  },
  "features": [
    {
      "id": "unit-suite",
      "name": "Default unit/contract suite (core purity, engine, api, cli, gui smoke, court deterministic, training units)",
      "status": "passing",
      "verification": "PYTHONPATH=src:tests $PY -m pytest -q -m \"not slow and not e2e\" -p no:cacheprovider",
      "evidence": "2026-09-07: 297 passed, 6 skipped, 27 deselected, 4 warnings (~20s) on cursor/artifact-registry-plan-5f28 (Linux py3.12). Artifact pack/fetch tests included; ONNX not in git."
    },
    {
      "id": "golden-cli-parity",
      "name": "CLI end-to-end golden parity on committed fixture clip (0.25s boundary tolerance)",
      "status": "passing",
      "verification": "PYTHONPATH=src:tests $PY -m pytest -q -p no:cacheprovider tests/test_cli_golden_parity.py",
      "evidence": "2026-08-24: 1 passed after regenerating tests/fixtures/golden_cli/golden_segments.csv for TCN hybrid decode (two segments 3.753-11.964 and 13.040-23.449 vs classic v0.4.0 one 3.800-24.000)."
    },
    {
      "id": "compileall",
      "name": "src+tests byte-compile cleanly",
      "status": "passing",
      "verification": "$PY -m compileall -q src tests",
      "evidence": "2026-08-24: exit 0 on feat/v0.5.0-tcn"
    },
    {
      "id": "tcn-v050-default",
      "name": "Ship TCN heatmap hybrid as default artifact (rallyclip_v0.5.0)",
      "status": "passing",
      "verification": "PYTHONPATH=src:tests $PY -m pytest -q -p no:cacheprovider tests/test_heatmap_decode.py tests/test_heatmap_runtime.py tests/test_runtime_api_engine.py tests/test_cli_golden_parity.py",
      "evidence": "2026-08-24: heatmap+pipeline tests 43 passed; golden CLI 1 passed; resolve_pipeline_spec(v0.5.0)=frame_startend_heatmap; v0.4.0 still hysteresis. Export parity max |torch-onnx|=1.88e-06."
    },
    {
      "id": "e2e-suite",
      "name": "Full e2e set: L1 backend golden jobs, L2 playwright UI, quality harness, court e2e (44 tests)",
      "status": "passing",
      "verification": "PYTHONPATH=src:tests $PY -m pytest -q -m e2e -p no:cacheprovider",
      "evidence": "2026-09-07: scheduler e2e skip checks stub source time 30s and paused=false so 25/35 autoplay seeks are OS-stable. Unit gate 280 passed. CI e2e confirmation pending this push. 2026-08-24 (feat/v0.5.0-tcn): L1 GUI e2e 5 passed; Playwright 2 passed (welcome + new-match). Golden-footage L1 still skips without data/e2e/aditi_5pts."
    },
    {
      "id": "onnx-pose-swap",
      "name": "Replace Ultralytics/torch pose extraction with torch-free ONNX Runtime runner via RuntimeDeps.PoseExtractor seam",
      "status": "passing",
      "verification": "Stage 3 of docs/onnx-pose-parity-plan.md: run CLI on golden clip with OnnxPoseExtractor injected; segments within existing 0.25s tolerance; then full unit + e2e gates",
      "evidence": "2026-07-04 (PR #26): runner at src/extraction/yolo_onnx_runner.py; manifest-referenced yolov8n-pose-960-dynamic.onnx; PoseExtractor+CourtDetector dispatch on extension; sweep 17/17 byte-equal (11 raw + 6 testing_app samples); golden CLI byte-equal in a venv WITHOUT torch; torch/ultralytics -> [train] extra. Perf 60s sample: onnx 19.7s/458MB vs torch 28.6s/753MB. Report: ../YOLO-ONNX/parity_960/sweep_report.json."
    },
    {
      "id": "frozen-app-e2e",
      "name": "Frozen .app smoke/e2e (rebuild v0.1.0-class app from refactored code, run L1 suite against binary)",
      "status": "passing",
      "verification": "Planned: build dist/RallyClip.app, dispatch --backend-only, reuse L1 suite; see docs/e2e_test_plan.md + docs/cli-in-release-binary-plan.md",
      "evidence": "2026-07-04: dist/RallyClip.app built from RallyClip.spec (torch-free, Qt-free after PR #27): 266MB .app / 113MB zipped (v0.1.0 dmg 558MB). Bundled --cli golden byte-equal; app boot -> /api/health 200 -> upload->analyze->library job with exact golden segments. --backend-only dispatch not built; smoke drives the real binary over HTTP."
    },
    {
      "id": "macos-release-cicd",
      "name": "CI/CD builds, signs, notarizes, and uploads the macOS desktop DMG",
      "status": "passing",
      "verification": "PYTHONPATH=src:tests $PY -m pytest -q tests/test_release_packaging.py -p no:cacheprovider",
      "evidence": "2026-09-07: rebased onto v0.5.0 main. release.yml tags v* run tests, fetch artifact-rallyclip_v0.5.0, pyinstaller RallyClip.spec (bundles DEFAULT_ARTIFACT_DIR=models/rallyclip_v0.5.0 + CFBundleIdentifier com.iroblesrazzaq.rallyclip), sign/DMG/notarytool/staple via scripts/release/, and upload RallyClip-<version>-macOS-arm64.dmg to a draft GitHub Release. tests/test_release_packaging.py 21 passed (spec lockstep, fetch/verify wiring, gitignore, no tracked ONNX)."
    },
    {
      "id": "webview-shell",
      "name": "System-webview desktop shell (pywebview WKWebView/WebView2) replacing QtWebEngine/PySide6",
      "status": "passing",
      "verification": "pytest -q tests/test_desktop_dispatch.py tests/test_native_playback.py; build via RallyClip.spec; driven-webview probe (play/seek/upload)",
      "evidence": "2026-07-04 (PR #27): gui/native_player.py + QWebChannel bridge deleted (frontend already fell back to HTML5 when the bridge was absent); WKWebView probe: canplay 1280x720, seek over ranges, playback advances, H.264+HEVC canPlayType 'probably', native file input. Bundle 765MB->266MB. CI 6/6 green."
    },
    {
      "id": "artifact-registry",
      "name": "ONNX off git: GitHub artifact zip + fetch; DMG still embeds DEFAULT_ARTIFACT_DIR",
      "status": "passing",
      "verification": "See docs/artifact-registry-plan.md Definition of done: depth-1 clone has no models/**/*.onnx; fetch_artifact.py then golden/e2e; CI fetch; PyInstaller bundle still contains v0.5.0 weights; packaged app does not download at launch.",
      "evidence": "2026-09-07: Published https://github.com/iroblesrazzaq/RallyClip/releases/tag/artifact-rallyclip_v0.5.0 (non-draft). git ls-files models has no *.onnx (manifests + SHA256SUMS only). PYTHONPATH=src:tests pytest -m 'not slow and not e2e': 297 passed, 6 skipped. python scripts/fetch_artifact.py no-ops when checksums match; hash mismatch raises ArtifactError. ci.yml fetch x2; release.yml fetch + verify_artifact_dir. RallyClip.spec still packs DEFAULT_ARTIFACT_DIR; frozen sources do not import runtime.artifact."
    }
  ],
  "active": "artifact-registry"
}